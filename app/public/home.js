$(document).ready(function () {

    $("#submitBtn").on("click", function(){

        alert("The button was clicked.");

        // take in values of the inputs
        const questionValues = $("#question").val();

        // some of this will need to be done in server side files.....

        // loop through the friends avatars

        // loop through the friends avatar's scores and add them

        // find differences between the scores

        // whoever has the smallest difference matches the user

        // display result in match.html
        
      });










});